export class EventLocation {
  constructor(
    public latitude: number,
    public longitude: number,
    public name: string,
    public address: string,
    public email: string,
    public url: string,
    public area: string
  ) {}
}